#!/usr/bin/env bash
# 在项目目录下启动仅监听 8080 的 HTTP 服务，用于查看餐饮分析报告前端
# 使用：./启动前端服务.sh 或 bash 启动前端服务.sh
# 浏览器访问：http://127.0.0.1:8080（不占用 8000/3000）

cd "$(dirname "$0")"
echo "前端服务启动在 http://127.0.0.1:8080（端口 8080，不影响 8000/3000）"
python -m http.server 8080
